from __future__ import annotations


def evaluate_answer(question_text: str, answer_text: str) -> dict[str, object]:
    """Stub evaluator reserved for the next implementation step."""

    raise NotImplementedError(
        "Evaluation logic is intentionally deferred. The scaffold keeps the module boundary in place."
    )
