from __future__ import annotations


def parse_resume_file(file_path: str) -> dict[str, object]:
    """Stub resume parser reserved for the next implementation step."""

    raise NotImplementedError(
        "Resume parsing is intentionally deferred. The scaffold keeps the module boundary in place."
    )
