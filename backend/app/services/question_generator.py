from __future__ import annotations


def generate_questions(resume_text: str, job_text: str) -> list[dict[str, str]]:
    """Stub question generator reserved for the next implementation step."""

    raise NotImplementedError(
        "Question generation is intentionally deferred. The scaffold keeps the module boundary in place."
    )
