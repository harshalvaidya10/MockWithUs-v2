from __future__ import annotations


def generate_embedding(text: str) -> list[float]:
    """Stub embedding generator reserved for the next implementation step."""

    raise NotImplementedError(
        "Embedding generation is intentionally deferred. The scaffold keeps the module boundary in place."
    )
