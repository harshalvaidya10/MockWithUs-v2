from __future__ import annotations


def compute_match_score(resume_embedding: list[float], job_embedding: list[float]) -> float:
    """Stub matcher reserved for the next implementation step."""

    raise NotImplementedError(
        "Matching logic is intentionally deferred. The scaffold keeps the module boundary in place."
    )
