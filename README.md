# MockWithUs Scaffold

This scaffold follows the clean monolith structure from the implementation plan.

## Quick start

1. Copy `backend/.env.example` to `backend/.env`.
2. Copy `frontend/.env.local.example` to `frontend/.env.local`.
3. Start the database and backend with Docker Compose.
4. Start the frontend locally with Node.js.
